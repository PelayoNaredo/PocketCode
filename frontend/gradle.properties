# Project-wide Gradle settings
android.useAndroidX=true
android.enableJetifier=true

# Increase build performance and fix JVM warnings
org.gradle.jvmargs=-Xmx4096m -XX:+UseG1GC -XX:MaxMetaspaceSize=768m -XX:+UseCompressedOops -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Kotlin code style for this project: "official" or "obsolete"
kotlin.code.style=official

# Enables namespacing of each library's R class so that its R class includes only the
# resources declared in the library itself and none from the library's dependencies,
# thereby reducing the size of the R class for that library
android.nonTransitiveRClass=true
org.gradle.configuration-cache=true

# KAPT and Hilt optimizations
kapt.incremental=true

# Suppress deprecated Gradle warnings for now
android.suppressUnsupportedCompileSdk=34
android.suppressKotlinVersionCompatibilityCheck=true
