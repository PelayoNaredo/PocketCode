// Build file for the `:features:project` module.
// This is an Android library module.
// Responsibilities:
// - Apply the `com.android.library` plugin.
// - Enable Jetpack Compose.
// - Depend on `:domain:project` to access use cases for project management.
// - Depend on `:core:ui` and `:core:utils`.
// - Depend on Hilt for dependency injection.
// - Depend on Jetpack Compose Navigation.
//
// This module encapsulates all UI and presentation logic related to project
// management, such as the project dashboard and file explorer.
