// Build file for the `:features:settings` module.
// This is an Android library module.
// Responsibilities:
// - Apply the `com.android.library` plugin.
// - Enable Jetpack Compose.
// - Depend on relevant `domain` modules if settings-related business logic exists.
// - Depend on `:core:ui` and `:core:utils`.
// - Depend on Hilt for dependency injection.
//
// This module contains the UI for all user-configurable settings, such as
// editor preferences, theme selection, and account management.
