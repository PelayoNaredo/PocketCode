// Build file for the `:features:editor` module.
// This is an Android library module.
// Responsibilities:
// - Apply the `com.android.library` plugin.
// - Enable Jetpack Compose.
// - Depend on the relevant `domain` modules (e.g., `:domain:ide`, `:domain:project`)
//   to access use cases.
// - Depend on shared `core` modules like `:core:ui` and `:core:utils`.
// - Depend on Hilt for injecting ViewModels and use cases.
// - Depend on Jetpack Compose Navigation for screen navigation.
//
// This module encapsulates everything related to the code editor feature.
