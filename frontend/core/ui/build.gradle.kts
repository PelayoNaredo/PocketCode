// Build file for the `:core:ui` module.
// This is an Android library module because it contains Android-specific UI components.
// Responsibilities:
// - Apply the `com.android.library` plugin.
// - Enable Jetpack Compose.
// - Declare dependencies on Compose libraries (e.g., `compose.ui`, `compose.material3`).
//
// This module provides a library of reusable UI components (e.g., custom buttons, dialogs),
// themes, colors, and typography for all feature modules to use, ensuring a consistent look and feel.
//
// This module will be a dependency for all `:features` modules.
