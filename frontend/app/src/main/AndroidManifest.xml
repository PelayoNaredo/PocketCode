<?xml version="1.0" encoding="utf-8"?>
<!--
This is the main Android Manifest file for the application.
It declares the fundamental components and properties of the app.

Responsibilities:
- Declare the application's package name.
- Define the application class (e.g., `com.pocketcode.PocketCodeApp`), which is the entry point for Hilt.
- Declare all activities, including the main launcher activity (`MainActivity`).
- Request necessary permissions (e.g., INTERNET, READ/WRITE_EXTERNAL_STORAGE).
- Define the application's icon, label, and theme.

This file is the root configuration for the Android OS to understand how to run the app.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pocketcode">

    <!-- Required for RevenueCat -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:name=".app.PocketCodeApp"
        android:label="@string/app_name"
        android:theme="@style/Theme.PocketCode"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android.supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PocketCode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
